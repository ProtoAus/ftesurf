// P385VERIFY -- pm_verify over p385rec.cfg's spectated run, the p382verify shape.
// NEEDS Patch 380's pm_verify (the `spec` record).
//   cd C:\FTESurf; C:\msys64\home\Lex\fteqw\engine\release\fteqwsv64.exe +set sv_public 0
//     +set sv_port 27699 +set log_enable 1 +set log_dir logs +set log_name p385verify
//     +set rec_enable 0 +set lobby_master "" +map surf_666 +exec cfg/test/p385verify.cfg
// (zones p360sf as maps/zones/local/surf_666.json, as for p385rec.cfg).  Run it on
// the Pi binary too.
// SUBJECT (untracked, data/p385/): s.rec, p385rec.cfg's S finish.
// DERIVED: n1mt.rec, `spec 0`'s <mt> + 1.
// PRE-REGISTERED
//   1  s PASS, "1 window(s)" with 0 lines inside.
//   2  CONTROL n1mt HOLD (the edge's counter against the replay's next row).
// FALSIFIED IF s does not PASS, or n1mt PASSes.
//
// RESULT (2026-09-20, engine\release\fteqwsv64 with Patch 380).  AS REGISTERED:
//   s PASS ticks 300 rows 301, "1 window(s), 0 line(s) inside"; n1mt HOLD "spec at
//   row 59: the edge states mover 3471 ..., the trace 3470 ...".  The Pi (aarch64
//   fteqw-svarm64 sha256 99712221, an overlay basedir with the fixture zones): identical.
echo ================================ P385 S
pm_verify data/p385/s.rec
echo ================================ P385 N1 mt
pm_verify data/p385/n1mt.rec
echo ================================ P385 done
quit
