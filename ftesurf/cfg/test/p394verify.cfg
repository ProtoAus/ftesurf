// P394VERIFY -- Patch 394: pm_verify on a run that crossed a relative trigger under
// the centre rule (data/p394/new.rec, recorded by p394rec.cfg on surf_sirius with
// the fixture zones installed at maps/zones/local/surf_sirius.json).
//
//   cd C:\FTESurf; C:\FTEQuake\fteqwsv64.exe +set sv_public 0 +set sv_port 27699
//     +set log_enable 1 +set log_dir logs +set log_name p394verify +set rec_enable 0
//     +set lobby_master "" +map surf_sirius +exec cfg/test/p394verify.cfg
//   (the fixture zones in place for the run; the Pi: the same over an overlay basedir)
//
// PRE-REGISTERED
//  V1 new.rec PASS ticks 50 rows 50.
//  V2 new_z8.rec (the telerel warp's z +8) HOLD on the state after that row: the
//     replay copies the recorded warp, so the rows after it no longer agree.
//  V3 the Pi's aarch64 binary gives the same verdicts and tick counts.
// FALSIFIED IF V1 is not PASS, V2 PASSes, or the Pi disagrees.
//
// RESULT (2026-09-20).  ALL HELD on the second recording.
//   Windows C:/FTEQuake/fteqwsv64: V1 PASS ticks 50 rows 50; V2 HOLD "state: 19
//   packet(s) differ, first at row 30".  Pi fteqw-svarm64 (sha256 99712221), an
//   overlay basedir, port 27697: the same two lines.
//   (First recording, warp on the final packet: its z+8 copy PASSed -- nothing
//   followed the warp to disagree, and the finish was still inside the zone.)
//   No old-rule recording verifies here: the Pi's three telerel files are REC 7.
echo ================================ P394 V1 new
pm_verify data/p394/new.rec
echo ================================ P394 V2 z+8
pm_verify data/p394/new_z8.rec
echo ================================ P394 done
quit
