cfg_save_auto 0
log_enable 1
log_dir logs
log_name p335j
cl_idlefps 0
cl_maxfps 100
vid_fullscreen 0
fs_missingwarn 0
sv_cheats 1
wait 200
echo === ahop_trick_or_treat ===
map ahop_trick_or_treat
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === bhop_angst ===
map bhop_angst
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === bhop_everfast ===
map bhop_everfast
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === bhop_mom_training_beta ===
map bhop_mom_training_beta
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === bhop_recharge ===
map bhop_recharge
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === df_cityrocket ===
map df_cityrocket
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === df_forward ===
map df_forward
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === df_momentum_training ===
map df_momentum_training
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === sj_mom_training ===
map sj_mom_training
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === surf_mom_training ===
map surf_mom_training
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === testbhop ===
map testbhop
waitmap 1800
waitms 2200
trig_dump
waitms 200
echo === conc_school ===
map conc_school
waitmap 1800
waitms 2200
trig_dump
waitms 200
quit
