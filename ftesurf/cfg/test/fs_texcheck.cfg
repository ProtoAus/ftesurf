// Written by tools/investigation: load one map with developer prints on, give
// the renderer time to fault in every texture the first frames ask for, quit.
// waitmap/waitms are Patch 164's real waits -- no frame-count guessing.
developer 2
map surf_tensor2
waitmap 60
waitms 8000
condump fs_texcheck.txt
quit
