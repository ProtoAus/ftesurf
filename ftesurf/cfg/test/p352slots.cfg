// PATCH 352 -- the physent digest no longer depends on the player slot count.
// No cfg body: run by the driver as a client-less dedicated server, twice:
//   fteqwsv64 -basedir C:\FTESurf +set sv_public 0 +set sv_port 27699 +set rec_enable 0
//     +set sv_playerslots <2|32> +set log_name p352slots<N> +map bhop_eazy +wait
//     +pm_verify data/b352fin.rec +pm_recsim data/b352warp.part
//     +pm_verify data/b88fin.rec +quit
// Subjects b352warp.part / b352fin.rec: b87warp.cfg / b88fin.cfg re-captured on a
// 2-slot listen server under the new digest (cfg/test/b352warp.cfg, b352fin.cfg).
//
// WHY: a live lobby file (32 slots) replayed on a default server (2 slots) matched
// ARM 4 exactly and mismatched the digest on every row; re-run with
// sv_playerslots 32 it matched 1126/1126.  Map entities are numbered after the
// reserved client slots, and the digest hashed that absolute number.
//
// PRE-REGISTERED: b352fin PASS and b352warp physents all-match at BOTH slot
// counts; the pre-352 control b88fin mismatches physents at both.
// RESULT (2026-09-18): HELD.  slots 2 and 32 alike: b352fin PASS ticks 660,
// physents 630/630; b352warp 1667/1667 exact, physents 1682/1682; b88fin
// physents 0/634 (file 1dba42, replay 88fd86) -> HOLD physents, ARM 4 exact.
