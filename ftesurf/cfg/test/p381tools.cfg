// PATCH 381 -- the evidence tools read Patch 382's `spec` record (a spectate hold).
// Tools only: no engine, no progs, nothing to run in the game.  reccheck and
// test_reccheck are written from the contract's grammar text for `spec` (the block
// over SV_RecOpen once 382 lands), never from the writer.
//
//   python tools/test_reccheck.py                              (the suite)
//   git show <pre-381>:tools/reccheck.py > <tmp>/old.py
//   RECCHECK=<tmp>/old.py python tools/test_reccheck.py case_spec   (CONTROL)
//   reccheck.py -v, old and new, over every .rec/.part under ftesurf/data; diff
//     the FAIL/fault/note lines (Windows: batch the files, the command line overflows)
//   python surfd/test_recplot.py            (stdlib; RECPLOT_CORPUS=ftesurf/data)
//   case_spec against the pre-381 surfd/recplot.py first on sys.path  (CONTROL)
//   SURFD_HOME=$(mktemp -d) python3 surfd/test_board.py        (Pi: needs Flask)
//
// RULES (reccheck, v9+; below v9 `spec` stays an unknown-record note)
//   FAULT  a malformed edge (11 values after `spec 1`, 11 + <why> after `spec 0`;
//          numeric, <mt> an integer, <fl> 0|1); a 0 with no 1 open; a 1 while one
//          is open; ANY line between a 1 and its 0 (a trailer line is faulted by
//          the next rule, or as never closed); the edges' <ticks> .. <fl> unequal
//          as numbers; the next `in` row or drop|rotate|server `pause` after a 0
//          not restating its <mt> <carry> -- past a `resume`/`retry` (a warm
//          rewind) only a <mt> BELOW the edge's.  A retry/load `pause` ends it.
//   FAULT  a `spec` line after `inend`/`end`, between a `pause` and its `session`,
//          or inside a ghost window (SV_SpecStart refuses all three; pm_verify
//          refuses the ghost one too).
//   FAULT, finished files only: TF_SPEC with no window, a window without
//          TF_SPEC, a window still open.  An unknown <why>: a NOTE.
//
// PRE-REGISTERED (2 and 5 re-registered at review; see RESULT)
//   1  test_reccheck: 0 failed, the case_spec arms included.
//   2  CONTROL, the pre-381 reccheck on case_spec: every FAULT arm reports no such
//      fault -- NO FAULT, or only the side effect of the inserted line (a trailer
//      count, the sample rate, a ghost flag); the %.9g exponent-form clean arms
//      FAULT there "in exponent form" (an old reccheck faults a real spec edge
//      printed that way: ship the tools before the progs).  `<mt>` in exponent
//      form is a PIN on EXP_OK_V9, not a control: it faults on both.
//   3  Corpus: data/runs fault lines identical old vs new; the only added output
//      is `spectated no`.  All of data/: identical except the 13 data/p380 files.
//   4  data/p380 (Patch 380's fixtures, flags 0): each faults "does not say
//      spectated"; scratch copies with TF_SPEC: S0 S1 0 faults, N1-N7 N9 N10
//      fault on their own edit, N8 (both edges tick 1049) passes -- nothing in
//      the grammar ties <ticks> to the trace; that is pm_verify's HOLD.
//   5  recplot: s1 gives one spec mark, held 20.0, why leave.  CONTROL: the
//      pre-381 recplot fails case_spec's four spec arms (the near-miss `spek` arm
//      is a pin: it passes on both).  admin_run.html under node with a fake DOM
//      names "spectated" in the flags row and draws the label; the pre-381
//      template fails the same four checks (control).
//   6  hidcheck, unchanged: `# <dt> spec 1|0 <ticks>` spliced into every real
//      .hid adds two "mark:" notes and nothing else.
//   7  (integrator, 382's live S1 capture) reccheck 0 faults, spectated yes,
//      spec_windows 1, spec_why leave.
// FALSIFIED IF a case_spec arm fails, a FAULT arm faults on the old reccheck for
//   the same reason, the corpus fault lines move, or 7 faults.
//
// RESULT (2026-09-19, worktree, Windows, Python 3.10)
//   First run (f548b22, 3323dff): 1 3 4 5 6 held.  2 FALSIFIED on one arm: `<mt>`
//   in exponent form faulted on the old reccheck for the same reason (EXP_RE),
//   and the sample-rate side effect was not listed; re-registered above.  The
//   recplot `spek` arm was labelled a control and passes on both; relabelled.
//   Review fixes (trailer, pause, ghost positions; warm-rewind >=; new arms):
//   1  229 checks, 0 failed (76 case_spec).  Mutation: 13 mutants of the spec
//      rules, 13 killed by case_spec.
//   2  old: 62 of 76 fail.  FAULT arms: NO FAULT, except in/sample/ghost inside
//      (a trailer count, the sample rate, TF_GHOST) and the `<mt>` pin.  The 13
//      other passes are clean arms (the old tool's "unknown record" notes are
//      not faults); the exponent-form clean arms FAULT.
//   3  data/runs 239 files, 143 FAIL / 144 fault lines both; +106 `spectated no`.
//      data/ 564 .rec + 13 .part: every difference in data/p380; faults and -v
//      notes identical to the first run's reccheck on all 577.
//   4  as predicted, N8 included; identical to the first run.
//   5  as predicted; pre-381 recplot: 4 of 5 case_spec arms fail.  6 as first run
//      (12 of 13 .hid; one has 2 frames).
//   7  open: needs 382.
echo p381tools: tools only -- see this file's header for the python arms
