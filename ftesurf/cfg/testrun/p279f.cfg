// Patch 279, probe F -- is the shield surface RASTERISED at all?
//
// After the two-pass restructure the shader reports `prog 1 passes 2`, so the
// program attached -- and the shield still draws nothing at seventy units where
// hl2_twotexture 0 draws a dense web.  Two possibilities remain and they need
// different fixes, so they get separated before either is worked on:
//
//   the fragment shader runs and both samplers are empty   -> solid RED here
//   the surface never reaches the rasteriser at all        -> nothing here
//
// The gamedir copy of glsl/vmt/twotexture.glsl overrides the plugin's, and is
// currently patched to write opaque red unconditionally.

vid_fullscreen 0
vid_width 1280
vid_height 720
vid_restart
waitms 3000
cl_maxfps 0
sv_cheats 1

map surf_tensor2
waitmap 600
waitms 9000
noclip
vbsp_sprites 0
vbsp_sprite_debug 2
waitms 1200
setpos -11776 -10886 13744 0 90 0
waitms 2500
screenshot p279f_red_x
waitms 900
screenshot p279f_red
waitms 900
quit
