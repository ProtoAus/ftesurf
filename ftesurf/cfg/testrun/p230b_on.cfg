// Patch 230, TEST arm: $staticprop models load as rigid geometry.  This is the
// default, so the cvar is set explicitly only to make the arm readable in the log.
log_enable 1
log_name p230b_on
hl2_rigidprops 1
exec cfg/testrun/p230b.cfg
