// Patch 241 / Build 39 ship check: all defaults, nothing set, three maps.
//
// Looking for: no QC runtime errors from the changed server files
// (sv_entities.qc's relative teleport, sv_saveloc.qc's load), the Patch 233/235
// material work still reporting 0 unresolved, and the new HUD source not
// producing console noise on a cold start.  hud_debug is left OFF because the
// point of this run is what a player sees with nothing configured.

log_enable 1
log_name p241ship
developer 1
log_developer 1
cfg_save_auto 0
cl_idlefps 0

wait 200
echo ^5=== p241ship: surf_demise ===
map surf_demise
waitmap 900
wait 300
hl2_unresolved
hl2_unresolved_visible
pm_jumpaddrise
hud_predicted

echo ^5=== p241ship: surf_aesthetic ===
map surf_aesthetic
waitmap 900
wait 300
hl2_unresolved
hl2_unresolved_visible

echo ^5=== p241ship: surf_sodacity ===
map surf_sodacity
waitmap 900
wait 300
hl2_unresolved
hl2_unresolved_visible

echo ^5=== p241ship done ===
wait 30
quit
