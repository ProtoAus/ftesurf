// FTESurf build 29 / Engine Patch 215 -- verification
//   1. the console drop-DOWN            (con_completiondown)
//   2. the completion list's row order   (arrows walked the wrong way)
//   3. lenient `map` completion          (sv_mapcompletion)
echo ^5=== b29a: Patch 215 ===^7

echo ^3-- the three new cvars, and Patch 213's still present --^7
con_completiondown
sv_mapcompletion
con_completionrows
con_displaypossibilities
con_gripsize

echo ^3-- both new cvars are writable in both directions --^7
con_completiondown 0
con_completiondown
sv_mapcompletion 0
sv_mapcompletion
con_completiondown 1
sv_mapcompletion 1

echo ^3-- a real map, so CSQC and the zones come up --^7
// wait for the startup command buffer to drain first, or `map` never runs
// (exec INSERTS at the front of the buffer -- build 19 lost three launches to this)
wait 200
map surf_aesthetic
waitmap 60
wait 60

echo ^3-- con_selftest: arrow gate, grip clamps, map leniency --^7
con_selftest

echo ^3-- regression --^7
pm_selftest
zone_selftest

echo ^3-- THE PICTURES: same typed line, list below then above --^7
toggleconsole
wait 20
con_settext map kits
wait 20
con_completiondown 1
wait 20
screenshot b29_map_dropdown
wait 20
con_completiondown 0
wait 20
screenshot b29_map_dropup
wait 20
con_settext cl_sh
wait 20
con_completiondown 1
wait 20
screenshot b29_cl_dropdown
wait 20
con_settext
wait 10
toggleconsole

echo ^5=== b29a done ===^7
exit
