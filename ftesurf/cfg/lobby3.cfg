// ---------------------------------------------------------------------------
//  cfg/lobby3.cfg -- the TIER 3 lobby.  Patch 275.
//  See cfg/lobby1.cfg for why the port is the identity and why they are spaced
//  by ten; only the three per-lobby values live here.
//
//  surf_borderlands is NOT here even though it is the map this server has been
//  sitting on since Stage 3 and is a tier 2.  It went because the lobby needed
//  a rotation rather than a permanent parking spot, and because it was reported
//  as playing badly; nothing has diagnosed WHY, so it is out of the rotation
//  rather than declared fixed.  surf_borderlands_h, its tier 3 sibling, is left
//  out for the same reason.
// ---------------------------------------------------------------------------
set sv_port     27530
set hostname    "FTESurf -- tier 3 (advanced)"

set lobby_maps  "surf_shady surf_aeron surf_inspire surf_lore surf_ember surf_amnytas"
set lobby_cycle 1800
